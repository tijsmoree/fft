# Fast Fourier Transform

This repository contains a small tool to calculate the FFT of a signal.

## Usage

The panel can be run using `pnpm`. `pnpm start` runs the application. It can be accessed using a modern browser on `localhost:8000`.
